<%=form_for :@myevent, :url=>{:action=>'education', :controller=>'myevents'} do |f|%>

	<%= f.submit'Education' %>

<% end  %>


<%=form_for :@myevent, :url=>{:action=>'pollution', :controller=>'myevents'} do |f|%>

	<%= f.submit'Pollution' %>

<% end  %>


<%=form_for :@myevent, :url=>{:action=>'food', :controller=>'myevents'} do |f|%>

	<%= f.submit'Food' %>

<% end  %>


<%=form_for :@myevent, :url=>{:action=>'environment', :controller=>'myevents'} do |f|%>

	<%= f.submit'Environment' %>

<% end  %>

